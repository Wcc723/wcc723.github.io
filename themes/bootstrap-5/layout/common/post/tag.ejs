<% if (post.tags && post.tags.length) { %>
    <%- list_tags(post.tags, {
        show_count: false,
        class: {
            ul: 'd-inline-block list-unstyled',
            li: 'd-inline-block me-1',
            a: 'badge bg-danger-soft text-danger mb-2'
        }
    }) %>
<% } %>